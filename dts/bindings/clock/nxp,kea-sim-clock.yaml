description: NXP Kinetis KEA SIM clock controller

compatible: "nxp,kea-sim-clock"

include: [base.yaml, clock-controller.yaml]

properties:
  reg:
    type: array
    required: true

  "#clock-cells":
    type: int
    const: 1

  clock-frequency:
    type: int
    required: true
    description: Peripheral bus clock frequency in Hz

clock-cells:
  - bits
